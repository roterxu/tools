spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/students?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC"
    username: "root"
    password: "1234"
    driverClassName: com.mysql.jdbc.Driver
server:
  port: 8888

mybatis-plus:
  config-location:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
